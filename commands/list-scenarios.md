---
description: List all WaterrAI scenarios in your active workspace
allowed-tools: "mcp__waterr-ai__list_scenarios"
---

Call the `list_scenarios` tool on the `waterr-ai` MCP server. Show the result as a compact table: id, name, persona, created_at. If the workspace is empty, say so and suggest creating one at waterr.ai.
