{
  "mcpServers": {
    "waterr-ai": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "https://waterr.ai/backend/mcp",
        "--header",
        "Authorization: Bearer ${WATERR_API_KEY}"
      ]
    }
  }
}
